language: node_js
sudo: false

node_js:
  - "0.10"
  - "0.12"
  - "4"
  # Node.js 5.x breaks with Analyze CSS
  #- "5"

after_success:
  - npm run coverage
  - npm i coveralls
  - cat ./coverage/lcov.info | coveralls

notifications:
  email:
    on_success: never
